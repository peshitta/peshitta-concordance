/** @module peshittaConcordance */

/**
 * Concordance for UBS Peshitta New Testament.
 * @static
 * @returns { object }  concordance hierarchical object
 */
const concordance = {};

export default concordance;

const test = require('assert');
const sut = require('../build/peshitta-concordance');

describe('Concordance', () => {
  it('Test Stub', () => {
    const keys = Object.keys(sut);
    test.strictEqual(keys.length, 0, 'dummy test');
  });
});
